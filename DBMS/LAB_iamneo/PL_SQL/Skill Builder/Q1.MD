DECLARE
  num1 NUMBER;
  num2 NUMBER;
  result NUMBER;
BEGIN

  SELECT NUMBER1, NUMBER2 INTO num1, num2 FROM NUMBERS WHERE NUMBER1 = 10 AND NUMBER2 = 20;

  result := num1 + num2;
  
  DBMS_OUTPUT.PUT_LINE('The sum of ' || num1 || ' and ' || num2 || ' is ' || result);
END;
/
