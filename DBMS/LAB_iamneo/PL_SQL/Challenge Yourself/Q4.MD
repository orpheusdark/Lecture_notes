DECLARE 

   v_number NUMBER:=359; 

   v_digit NUMBER; 

   v_sum NUMBER := 0; 

BEGIN 

   WHILE v_number > 0 LOOP 

      v_digit := MOD(v_number, 10); 

      v_sum := v_sum + v_digit; 

      v_number := FLOOR(v_number / 10); 

   END LOOP; 
