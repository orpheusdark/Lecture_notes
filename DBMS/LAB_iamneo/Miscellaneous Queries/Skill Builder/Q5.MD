SELECT ORDER_ID, TO_CHAR(ORDER_DATE, 'Day') AS ORDER_DAY
FROM ORDERS;

SELECT TO_CHAR(DELIVERY_DATE, 'Month') AS DELIVERY_MONTH, TO_CHAR(DELIVERY_DATE, 'DD') AS DELIVERY_DAY
FROM ORDERS;

SELECT TO_DATE('2008-05-25', 'YYYY-MM-DD') + 25 AS DATE_AFTER_25_DAYS
FROM DUAL;
